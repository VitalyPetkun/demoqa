package elements;

import framework.BaseElement;
import org.openqa.selenium.By;

public class DatePicker extends BaseElement {
    public DatePicker(By locator, String name) {
        super(locator, name);
    }
}
